#include <iostream>
using namespace std;

int main() {
    bool a = true;
    bool b = false;

    cout << "a AND b (a && b): " << (a && b) << endl;
    cout << "a OR b  (a || b): " << (a || b) << endl;
    cout << "NOT a   (!a): " << (!a) << endl;

    return 0;
}
